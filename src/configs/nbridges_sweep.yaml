entity: shreyaspadhy
project: cais
program: main.py

method: grid
parameters:
  boundmode:
    values: ['MCD_CAIS_sn']
  model:
    # values: ['log_sonar', 'log_ionosphere', 'lorenz', 'brownian', 'banana', 'seeds']
    values: ['funnel']
  nbridges:
    values: [256, 128, 64, 32, 16, 8]
  N:
    values: [300]
  iters:
    values: [11000]
  pretrain_mfvi:
    values: [False]
  train_eps:
    values: [False]
  emb_dim:
    values: [48]
  n_samples:
    values: [2000]
  

command:
  - python
  - ${program}
  - --config
  - configs/base.py